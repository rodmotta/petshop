spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/petshop
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update # create-drop | validate | none | create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/
logging:
  level:
    org:
      springframework:
        security: DEBUG